// Initialisation du système de chat d'annonces
document.addEventListener('DOMContentLoaded', function () {
  console.log('🚀 Initialisation AdChatManager...');
  window.adChatManager = new AdChatManager();
  console.log('✅ AdChatManager initialisé');
});
